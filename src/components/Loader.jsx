import styles from './Loader.module.css';

const Loader = () => {
  return (
    <div className={styles.title}>
      <p>Loading data is underway...</p>
    </div>
  )
}

export default Loader;